{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://accescom-production.up.railway.app/api/:path*"
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "env": {
    "GENERATE_SOURCEMAP": "false"
  }
}
